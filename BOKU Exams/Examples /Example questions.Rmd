```{r data generation}
data <- sample(1:20, size = 10, replace = TRUE)

questions <- list()
solutions <- list() 
explanations <- list() 
types <- list()
tolerances <- list()

#Question 1 
solutions[[1]] <- mean(data)
types[[1]] <- "num"

#Question 2 
solutions[[2]] <- "variance"
types[[2]] <- "string"

#Question 3 
questions[[3]] <- c("The mean is a measure of central tendency.", "The mean is a measure of measure of dispersion.", "The mean is affected by outliers.", "The mean is unaffected by outliers.")
solutions[[3]] <- c(TRUE, FALSE, TRUE, FALSE)
types[[3]] <- "mchoice"

#Question 4 
questions[[4]] <- c("yes", "no")
solutions[[4]] <- c(TRUE, FALSE)
types[[4]] <- "schoice"

```

Question
========
What is the mean of the data? ##ANSWER1## 

What is the paramter $\sigma ^2$, which is the standard deviation squared, called? ##ANSWER2##

Please select the correct statments: ##ANSWER3##

Is the following formula for the arithmetic mean correct? 
$$
\bar{x} = \frac{1}{n}\sum_{i = 1}^n{x_i}

$$ ##ANSWER4##

Meta-information
================ 
exname: 
exsolution: `r paste(solutions, sep = "|")`



